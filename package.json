{
  "name": "habitx-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Habit X public API / BFF — online-first habit domain service",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "openapi:check": "node -e \"require('fs').accessSync('openapi/habitx-v1.yaml')\""
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
